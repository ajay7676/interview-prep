import React from 'react'
import Item from './Item'

const TodoList = () => {
  return (
    <ol className="todo_list">
        {/* <Item key={id} item={item} setTodos={setTodos} /> */}
    </ol>
  )
}

export default TodoList